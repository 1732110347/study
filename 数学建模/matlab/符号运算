eval 函数用于将符号表达式转换成数值表达式eval(10/2)
sym 将数值表达式转换成符号表达式sym(3.4)

Digits(D)设置有效数字个数为D的近似解精度
vpa(S)符号表达式S在digits函数设置的精度下的数值解
vpa(S，D)符号表达式在digits函数设置的有效数字个数为D的近似解精度下的数值解


sym创建符号矩阵
sym('x')创建变量符号x
sym('a',[n1...NM])创建n1-nM自动生成元素
sym('A'n)创建一个nxn符号矩阵，自动
sym('a'n)创建一个由n个自动生成的元素组成的符号数组
sym(  ,set)通过set设置符号表达式的格式


符号矩阵的转置运算
B=A.'
B=transpose(A)
符号矩阵的行列式计算 d=det(A)
逆运算int(A)
求秩rank(A)
特征值eig
特征向量eigensys
奇异值svd singavals
若尔当jordan

因式分解factor
符号矩阵的展开expand

符号简化
simple(S)若S为一矩阵，则结果是全矩阵的最短型，而非每个元素的最短型
[r how]=simple(S)返回的r为简化型，how为简化过程中使用的方法
simplify简化符号矩阵的每一个元素

分式通分[n，d]=numden(A)把A的各元素转换为分子和分母都是整系数的最佳多项式型

秦九韶型重写horner(P)




























